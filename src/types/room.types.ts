export type SummaryRoom = {
  id: string
  nome: string
  capacidade: number
}

export type SalaDetalhes = {
  id: string
  nome: string
  capacidade: number
  localizacao: string
  descricao: string
}
