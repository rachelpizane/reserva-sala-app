export type RoomSummary = {
  id: string
  nome: string
  capacidade: number
}

export type RoomDetails = {
  id: string
  nome: string
  capacidade: number
  localizacao: string
  descricao: string
}
